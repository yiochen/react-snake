import React, { Component } from 'react';

export default class SceneSwitcher extends Component {
    constructor(props) {
        super(props);
        this.state = {
            scene: this.props.currentScene
        }
    }

    onChange 
    render() {

    }
}

export default App = <SceneSwitcher />;